// Test generated by RoostGPT for test test1 using AI Model gpt

```go
package main

import (
	"net/http"
	"net/http/httptest"
	"strings"
	"testing"
)

func TestMain2b8a936782(t *testing.T) {
	t.Run("TestRootEndpoint", func(t *testing.T) {
		request, _ := http.NewRequest(http.MethodGet, "/", nil)
		response := httptest.NewRecorder()

		serveRoot(response, request)

		got := response.Body.String()
		want := "Root endpoint is working" // TODO: Change this to the expected output

		if !strings.Contains(got, want) {
			t.Errorf("got %q, want %q", got, want)
		}
	})

	t.Run("TestRunTestEndpoint", func(t *testing.T) {
		request, _ := http.NewRequest(http.MethodGet, "/tests/run", nil)
		response := httptest.NewRecorder()

		runTest(response, request)

		got := response.Body.String()
		want := "Test is running" // TODO: Change this to the expected output

		if !strings.Contains(got, want) {
			t.Errorf("got %q, want %q", got, want)
		}
	})
}