// Test generated by RoostGPT for test test1 using AI Model gpt

```go
package main

import (
	"log"
	"net"
	"net/http"
	"testing"
	"time"
)

func TestMain2b8a936782(t *testing.T) {
	// start the server in goroutine
	go main()

	// wait for the server to start
	time.Sleep(time.Second)

	// test the root endpoint
	resp, err := http.Get("http://localhost" + net.JoinHostPort("", port))
	if err != nil {
		t.Error("Failed to send request to the server:", err)
		return
	}
	if resp.StatusCode != http.StatusOK {
		t.Error("Expected status OK, got", resp.StatusCode)
	}

	// test the /tests/run endpoint
	resp, err = http.Get("http://localhost" + net.JoinHostPort("", port) + "/tests/run")
	if err != nil {
		t.Error("Failed to send request to the server:", err)
		return
	}
	if resp.StatusCode != http.StatusOK {
		t.Error("Expected status OK, got", resp.StatusCode)
	}
}