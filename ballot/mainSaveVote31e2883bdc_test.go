// Test generated by RoostGPT for test test1 using AI Model gpt

```go
package main

import (
	"testing"
)

type Vote struct {
	CandidateID string
}

var candidateVotesStore map[string]int

func getCandidatesVote() map[string]int {
	return candidateVotesStore
}

func saveVote(vote Vote) error {
	candidateVotesStore = getCandidatesVote()
	candidateVotesStore[vote.CandidateID]++
	return nil
}

func TestSaveVote31e2883bdc(t *testing.T) {
	// Initialize candidateVotesStore
	candidateVotesStore = make(map[string]int)

	// Test case 1: New vote
	vote := Vote{CandidateID: "candidate1"}
	err := saveVote(vote)
	if err != nil {
		t.Error("Failed to save vote:", err)
	}
	if candidateVotesStore[vote.CandidateID] != 1 {
		t.Error("Vote count not incremented correctly for new vote")
	}

	// Test case 2: Increment existing vote
	err = saveVote(vote)
	if err != nil {
		t.Error("Failed to save vote:", err)
	}
	if candidateVotesStore[vote.CandidateID] != 2 {
		t.Error("Vote count not incremented correctly for existing vote")
	}
}